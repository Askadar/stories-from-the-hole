---
import MDPostLayout from '@/layouts/MDPostLayout.astro'

const { entry } = Astro.props
const { Content } = await entry.render()

import { getPosts } from './getPaths'
export const getStaticPaths = getPosts
---

<MDPostLayout title={entry.data.title}>
	<Content />
</MDPostLayout>
