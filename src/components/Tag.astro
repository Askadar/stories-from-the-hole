---
import { url } from '@/util'
export interface Props {
	label: string
}

const { label } = Astro.props
---

<a href={url(`/tags/${label}/1`)} class="flex gap-0.5">
	<span class="font-semibold">#</span><span>{label}</span>
</a>
